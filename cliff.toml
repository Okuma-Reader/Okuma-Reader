# https://git-cliff.org/docs/configuration
# Generates changelogs from Conventional Commits (feat/fix/chore/…).

[changelog]
header = """# Changelog

All notable changes to this project will be documented in this file.

"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits %}
- {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
{% if commit.breaking %}**breaking:** {% endif %}\
{{ commit.message | upper_first }}\
{% endfor %}
{% endfor %}

"""
footer = """
{% for release in releases -%}
{% if release.version and release.previous.version -%}
[{{ release.version | trim_start_matches(pat="v") }}]: https://github.com/Okuma-Reader/Okuma-Reader/compare/{{ release.previous.version }}...{{ release.version }}
{% endif -%}
{% endfor %}
"""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
protect_breaking_commits = true
commit_parsers = [
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactor" },
  { message = "^doc", group = "Documentation" },
  { message = "^chore\\(release\\)", skip = true },
  { message = "^chore\\(changelog\\)", skip = true },
  { message = "^Merge ", skip = true },
  { message = "^chore|^ci", group = "Miscellaneous" },
  { message = "^test", group = "Testing" },
  { message = "^style", group = "Styling" },
  { message = "^revert", group = "Reverts" },
]
filter_commits = false
sort_commits = "newest"
topo_order = false
